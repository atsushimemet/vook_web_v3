<section class='admin-section'>
  <%= render partial: 'shared/section_headline', locals: { title: 'Item' } %>
  <div class='admin-container container'>
    <table class='table preview-table is-bordered is-fullwidth'>
      <tr>
        <th>アイテム名</th>
        <td><%= @item.name %></td>
      </tr>
      <tr>
        <th>アイコン</th>
        <td>
          <% if @item.image.attached? %>
            <%= image_tag @item.image %>
          <% else %>
            なし
          <% end %>
        </td>
      </tr>
      <tr>
        <th>バナー</th>
        <td>
          <% if @item.banner.attached? %>
            <%= image_tag @item.banner %>
          <% else %>
            なし
          <% end %>
        </td>
      </tr>
    </table>
    <hr class='has-background-dark'>
    <%= render 'form', item: @item %>
    <%= link_to '管理画面に戻る', admin_path %> |
    <%= link_to "#{@item.name}のページへ", @item %>
  </div>
</section>
